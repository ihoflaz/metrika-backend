<!DOCTYPE html>
<html lang="tr">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <meta name="subject" content="📊 Haftalık Özet Raporu - {{weekOf}}" />
  <style>
    body {
      font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, 'Helvetica Neue', Arial, sans-serif;
      line-height: 1.6;
      color: #333;
      max-width: 700px;
      margin: 0 auto;
      padding: 20px;
      background-color: #f5f5f5;
    }
    .container {
      background-color: white;
      border-radius: 8px;
      padding: 30px;
      box-shadow: 0 2px 8px rgba(0, 0, 0, 0.1);
    }
    .header {
      text-align: center;
      padding-bottom: 20px;
      border-bottom: 2px solid #6366f1;
    }
    .logo {
      font-size: 24px;
      font-weight: bold;
      color: #6366f1;
    }
    h1 {
      color: #111;
      font-size: 24px;
      margin: 20px 0;
    }
    h2 {
      color: #6366f1;
      font-size: 18px;
      margin: 25px 0 15px 0;
      padding-bottom: 10px;
      border-bottom: 2px solid #e0e7ff;
    }
    .stats-grid {
      display: grid;
      grid-template-columns: repeat(2, 1fr);
      gap: 15px;
      margin: 20px 0;
    }
    .stat-card {
      background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
      color: white;
      padding: 20px;
      border-radius: 8px;
      text-align: center;
    }
    .stat-card.green {
      background: linear-gradient(135deg, #10b981 0%, #059669 100%);
    }
    .stat-card.yellow {
      background: linear-gradient(135deg, #f59e0b 0%, #d97706 100%);
    }
    .stat-card.red {
      background: linear-gradient(135deg, #ef4444 0%, #dc2626 100%);
    }
    .stat-number {
      font-size: 36px;
      font-weight: bold;
      margin: 10px 0;
    }
    .stat-label {
      font-size: 14px;
      opacity: 0.9;
    }
    .task-list {
      margin: 15px 0;
    }
    .task-item {
      background-color: #f9fafb;
      border-left: 4px solid #6366f1;
      padding: 12px;
      margin: 10px 0;
      border-radius: 4px;
    }
    .task-item.urgent {
      border-left-color: #ef4444;
      background-color: #fef2f2;
    }
    .task-title {
      font-weight: 600;
      color: #111;
      margin-bottom: 5px;
    }
    .task-meta {
      font-size: 12px;
      color: #666;
    }
    .kpi-item {
      display: flex;
      justify-content: space-between;
      align-items: center;
      padding: 10px;
      margin: 8px 0;
      background-color: #f9fafb;
      border-radius: 4px;
    }
    .kpi-name {
      font-weight: 600;
    }
    .kpi-value {
      font-size: 18px;
      font-weight: bold;
    }
    .kpi-value.good {
      color: #10b981;
    }
    .kpi-value.warning {
      color: #f59e0b;
    }
    .kpi-value.bad {
      color: #ef4444;
    }
    .cta-button {
      display: inline-block;
      padding: 12px 24px;
      background-color: #6366f1;
      color: white;
      text-decoration: none;
      border-radius: 6px;
      font-weight: 600;
      margin: 20px 0;
      transition: background-color 0.2s;
    }
    .cta-button:hover {
      background-color: #4f46e5;
    }
    .footer {
      margin-top: 30px;
      padding-top: 20px;
      border-top: 1px solid #e5e5e5;
      text-align: center;
      color: #999;
      font-size: 12px;
    }
    .message {
      color: #666;
      line-height: 1.8;
      margin: 20px 0;
    }
  </style>
</head>
<body>
  <div class="container">
    <div class="header">
      <div class="logo">📊 Metrika</div>
      <h1>Haftalık Özet Raporu</h1>
      <p style="color: #666; margin: 10px 0;">{{weekOf}}</p>
    </div>

    <div class="message">
      <p>Merhaba <strong>{{userName}}</strong>,</p>
      <p>Bu haftanın proje ve görev özetiniz aşağıdadır:</p>
    </div>

    <h2>📊 Haftalık İstatistikler</h2>
    <div class="stats-grid">
      <div class="stat-card green">
        <div class="stat-number">{{completedTasks}}</div>
        <div class="stat-label">Tamamlanan Görev</div>
      </div>
      <div class="stat-card">
        <div class="stat-number">{{activeTasks}}</div>
        <div class="stat-label">Devam Eden Görev</div>
      </div>
      <div class="stat-card yellow">
        <div class="stat-number">{{upcomingDeadlines}}</div>
        <div class="stat-label">Yaklaşan Deadline</div>
      </div>
      <div class="stat-card red">
        <div class="stat-number">{{delayedTasks}}</div>
        <div class="stat-label">Geciken Görev</div>
      </div>
    </div>

    {{#if urgentTasks}}
    <h2>🚨 Acil Dikkat Gerektiren Görevler</h2>
    <div class="task-list">
      {{#each urgentTasks}}
      <div class="task-item urgent">
        <div class="task-title">{{this.title}}</div>
        <div class="task-meta">
          {{this.projectName}} • {{this.delayDays}} gün gecikmiş
        </div>
      </div>
      {{/each}}
    </div>
    {{/if}}

    {{#if upcomingTasks}}
    <h2>📅 Bu Hafta Tamamlanması Gereken Görevler</h2>
    <div class="task-list">
      {{#each upcomingTasks}}
      <div class="task-item">
        <div class="task-title">{{this.title}}</div>
        <div class="task-meta">
          {{this.projectName}} • Bitiş: {{formatDate this.plannedEnd}}
        </div>
      </div>
      {{/each}}
    </div>
    {{/if}}

    {{#if kpiSummary}}
    <h2>📈 KPI Özeti</h2>
    {{#each kpiSummary}}
    <div class="kpi-item">
      <div class="kpi-name">{{this.name}}</div>
      <div class="kpi-value {{this.status}}">
        {{this.currentValue}} / {{this.targetValue}}
      </div>
    </div>
    {{/each}}
    {{/if}}

    <h2>🎯 Haftalık Hedefler</h2>
    <div class="message">
      <ul>
        <li><strong>{{completedTasks}}</strong> görev tamamlandı (Hedef: {{weeklyGoal}})</li>
        {{#if (gt delayedTasks 0)}}
        <li style="color: #dc2626;">⚠️ <strong>{{delayedTasks}}</strong> geciken göreve müdahale edin</li>
        {{/if}}
        {{#if (gt upcomingDeadlines 0)}}
        <li style="color: #d97706;">📅 <strong>{{upcomingDeadlines}}</strong> yaklaşan deadline'a odaklanın</li>
        {{/if}}
        <li>✅ Tüm görevleriniz zamanında!</li>
      </ul>
    </div>

    <div style="text-align: center;">
      <a href="{{dashboardUrl}}" class="cta-button">Dashboard'a Git</a>
    </div>

    <div class="footer">
      <p>Bu haftalık özet her Pazartesi sabahı otomatik olarak gönderilir.</p>
      <p>© {{currentYear}} Metrika - Proje Yönetim Platformu</p>
    </div>
  </div>
</body>
</html>
