<!DOCTYPE html>
<html lang="tr">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <meta name="subject" content="⚠️ Görev Eskalasyonu: {{taskTitle}}" />
  <style>
    body {
      font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, 'Helvetica Neue', Arial, sans-serif;
      line-height: 1.6;
      color: #333;
      max-width: 600px;
      margin: 0 auto;
      padding: 20px;
      background-color: #f5f5f5;
    }
    .container {
      background-color: white;
      border-radius: 8px;
      padding: 30px;
      box-shadow: 0 2px 8px rgba(0, 0, 0, 0.1);
    }
    .header {
      text-align: center;
      padding-bottom: 20px;
      border-bottom: 2px solid #ef4444;
    }
    .logo {
      font-size: 24px;
      font-weight: bold;
      color: #6366f1;
    }
    .alert-badge {
      display: inline-block;
      padding: 8px 16px;
      border-radius: 6px;
      font-weight: 600;
      margin: 20px 0;
      font-size: 14px;
    }
    .alert-critical {
      background-color: #fee2e2;
      color: #dc2626;
      border: 1px solid #fca5a5;
    }
    .alert-warning {
      background-color: #fef3c7;
      color: #d97706;
      border: 1px solid #fcd34d;
    }
    h1 {
      color: #111;
      font-size: 24px;
      margin: 20px 0;
    }
    .task-info {
      background-color: #fef2f2;
      border-left: 4px solid #ef4444;
      padding: 15px;
      margin: 20px 0;
      border-radius: 4px;
    }
    .info-row {
      display: flex;
      justify-content: space-between;
      padding: 8px 0;
      border-bottom: 1px solid #fee2e2;
    }
    .info-row:last-child {
      border-bottom: none;
    }
    .info-label {
      font-weight: 600;
      color: #991b1b;
    }
    .info-value {
      color: #333;
    }
    .cta-button {
      display: inline-block;
      padding: 12px 24px;
      background-color: #ef4444;
      color: white;
      text-decoration: none;
      border-radius: 6px;
      font-weight: 600;
      margin: 20px 0;
      transition: background-color 0.2s;
    }
    .cta-button:hover {
      background-color: #dc2626;
    }
    .message {
      color: #666;
      line-height: 1.8;
      margin: 20px 0;
    }
    .footer {
      margin-top: 30px;
      padding-top: 20px;
      border-top: 1px solid #e5e5e5;
      text-align: center;
      color: #999;
      font-size: 12px;
    }
  </style>
</head>
<body>
  <div class="container">
    <div class="header">
      <div class="logo">📊 Metrika</div>
      <h1>⚠️ Görev Eskalasyonu</h1>
    </div>

    {{#if (eq escalationLevel "critical")}}
      <div class="alert-badge alert-critical">
        🚨 KRİTİK ESKALASYON
      </div>
    {{else}}
      <div class="alert-badge alert-warning">
        ⚠️ UYARI ESKALASYONU
      </div>
    {{/if}}

    <div class="message">
      <p>Merhaba <strong>{{escalatedToName}}</strong>,</p>
      
      <p>
        Aşağıdaki görev <strong>{{delayDays}} gündür</strong> gecikmiş durumda ve size eskalasyon yapıldı.
        Lütfen acil müdahale ediniz.
      </p>
    </div>

    <div class="task-info">
      <div class="info-row">
        <span class="info-label">Görev:</span>
        <span class="info-value">{{taskTitle}}</span>
      </div>
      <div class="info-row">
        <span class="info-label">Proje:</span>
        <span class="info-value">{{projectName}}</span>
      </div>
      <div class="info-row">
        <span class="info-label">Görev Sahibi:</span>
        <span class="info-value">{{ownerName}}</span>
      </div>
      <div class="info-row">
        <span class="info-label">Gecikme Süresi:</span>
        <span class="info-value" style="color: #dc2626; font-weight: bold;">{{delayDays}} gün</span>
      </div>
      <div class="info-row">
        <span class="info-label">Eskalasyon Seviyesi:</span>
        <span class="info-value">
          {{#if (eq escalationLevel "critical")}}
            🚨 Kritik
          {{else}}
            ⚠️ Uyarı
          {{/if}}
        </span>
      </div>
    </div>

    <div class="message">
      <p><strong>Beklenen Aksiyon:</strong></p>
      <ul>
        <li>Görev durumunu ve gecikme nedenini inceleyin</li>
        <li>Görev sahibi ile görüşün</li>
        <li>Gerekli kaynakları sağlayın</li>
        <li>Yeni tamamlanma tarihini belirleyin</li>
      </ul>
    </div>

    <div style="text-align: center;">
      <a href="{{taskUrl}}" class="cta-button">Görevi Görüntüle ve Müdahale Et</a>
    </div>

    <div class="footer">
      <p>Bu bir otomatik eskalasyon bildirimidir.</p>
      <p>© {{currentYear}} Metrika - Proje Yönetim Platformu</p>
    </div>
  </div>
</body>
</html>
